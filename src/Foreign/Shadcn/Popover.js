import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export const popoverImpl = Popover
export const popoverContentImpl = PopoverContent
export const popoverTriggerImpl = PopoverTrigger
