import {
  Alert,
  AlertDescription,
  AlertTitle,
} from "@/components/ui/alert"

export const alertImpl = Alert
export const alertTitleImpl = AlertTitle
export const alertDescriptionImpl = AlertDescription
