import { 
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  CardDescription,
  CardFooter
 } from "@/components/ui/card"

export const cardImpl = Card
export const cardHeaderImpl = CardHeader
export const cardTitleImpl = CardTitle
export const cardDescriptionImpl = CardDescription
export const cardContentImpl = CardContent
export const cardFooterImpl = CardFooter
